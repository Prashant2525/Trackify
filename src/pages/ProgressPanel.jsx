import React from "react";

const ProgressPanel = () => (
  <div className="progress-panel">
    <p>
      Progress will be shown here only after you join any project and only when professor assigns you a weekly task
    </p>
  </div>
);

export default ProgressPanel;
